import { expect } from "@std/expect";
import { test } from "@std/testing/bdd";

// モジュールが正しくインポートできることを確認するテスト
test("todo-cli module can be imported", () => {
  // このテストは常に成功し、単にテストファイルが存在することを確認するためのもの
  expect(true).toBe(true);
});
