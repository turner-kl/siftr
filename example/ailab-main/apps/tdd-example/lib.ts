/**
 * 足し算を行う関数
 * @param a
 * @param b
 * @returns a + b
 */
export function add(a: number, b: number): number {
  return a + b;
}

export function sub(a: number, b: number): number {
  return a - b;
}
